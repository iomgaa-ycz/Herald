{{ static_fragments_text | default('', true) }}

# 当前阶段

你当前处于 `draft_execute` phase，需要把描述态方案实现为完整可运行代码。

{% if agent %}
# 当前执行 Agent

- `name`: `{{ agent.name }}`
- `display_name`: `{{ agent.display_name }}`

{% if agent.prompt_text %}
## Agent 执行偏好

{{ agent.prompt_text }}
{% endif %}
{% endif %}

# 角色边界

- `agent` 负责执行风格与调试策略。
- `task_spec`、`solution`、`workspace` 才是本轮实现的硬约束。
- 不要把 Agent 提示重写成新的任务目标；若与当前方案冲突，以当前任务上下文为准。

{% if task_spec %}
# 任务规格

- `task_type`: `{{ task_spec.task_type }}`
- `competition_name`: `{{ task_spec.competition_name }}`
- `objective`: `{{ task_spec.objective }}`
- `metric_name`: `{{ task_spec.metric_name }}`
- `metric_direction`: `{{ task_spec.metric_direction }}`
{% endif %}

{% if data_profile %}
# 数据概况

{{ data_profile }}
{% else %}
# 数据概况

- 未提供 `data_profile`，请基于 `task_spec` 与实际文件结构做最小可运行实现。
{% endif %}

{% if workspace %}
# 工作空间

- `workspace_root`: `{{ workspace.workspace_root }}`
- `data_dir`: `{{ workspace.data_dir }}`
- `working_dir`: `{{ workspace.working_dir }}`
- `logs_dir`: `{{ workspace.logs_dir }}`
- `db_path`: `{{ workspace.db_path }}`
- `run_log_path`: `{{ workspace.run_log_path }}`
{% else %}
# 工作空间

- 未提供 `workspace`，请在当前工作目录完成实现，并自行保持文件结构清晰。
{% endif %}

{% if recent_error %}
# 最近错误

{{ recent_error }}
{% endif %}

{% if solution.plan_summary %}
# 当前方案的 Plan 摘要

{{ solution.plan_summary }}
{% endif %}

{% if solution.genes %}
# 当前方案的 Gene 描述

{% for slot_name, gene in solution.genes.items() %}
## Slot `{{ slot_name }}`

{{ gene }}

{% endfor %}
{% else %}
# 当前方案的 Gene 描述

- 当前 `solution.genes` 为空，请根据 plan 摘要与任务规格完成最小可运行实现。
{% endif %}

{% if template_content %}
# 模板骨架

```python
{{ template_content }}
```
{% endif %}

{% if allowed_tools is defined %}
# 当前 phase 可见工具

{% if allowed_tools %}
- {{ allowed_tools | join(" / ") }}
{% else %}
- 当前 phase 无额外工具。
{% endif %}
{% endif %}

# 可用工具

- 基础库: numpy, pandas, scipy
- 机器学习: scikit-learn, xgboost, lightgbm
- 深度学习: torch, torchvision, timm
- 优化: optuna
- 推荐优先使用 PyTorch 进行深度学习任务

# 执行步骤

你必须按以下编号顺序完成所有步骤，**不得跳过任何步骤**。
若存在最近错误，先定位并解除阻塞，再继续推进。

## Step 1: 编写 solution.py

- 把描述态方案实现为完整、自包含、可直接运行的代码，写入 `{{ workspace.working_dir if workspace else "." }}/solution.py`
- 先保证 MVP baseline 跑通，再做必要优化
- 所有实现围绕 `{{ task_spec.metric_name if task_spec else "unknown" }}` ({{ task_spec.metric_direction if task_spec else "maximize" }}) 展开
- 必须结合上方 `data_profile` 选择合适的数据读取、缺失值处理、特征编码与验证策略；必须按 §6/§7 设置资源参数，禁止 n_jobs=-1
- 必须在验证集上计算并打印评估指标
- 必须将测试集预测结果保存为 `submission.csv`
- 若 gene 描述与 plan 摘要存在不一致，以更具体、更新的方案为准

## Step 2: 执行 solution.py（禁止跳过）

使用以下命令运行，将输出同时保存到日志文件：

```bash
set -o pipefail; python -u solution.py 2>&1 | tee -a {{ workspace.run_log_path if workspace else "run.log" }}
```

等待运行完成，记录输出中的评估指标值。

## Step 3: 检查与修正

- 若 Step 2 运行失败（语法错误、ImportError、文件路径错误、数据类型不匹配等），**必须修复代码后重新执行 Step 2**
- 若运行成功但未生成 submission.csv 或指标值异常，修复后重新执行
- 确认 submission.csv 存在且行数与测试集匹配
- 确认验证集指标值合理（非 NaN、非零、在合理范围内）

# 输出格式

你必须严格按以下结构输出，不能包含任何额外标题或多余文本：

## 执行报告
（1-3 段自然语言，描述实际实现内容）

## 代码实现
```python
# 完整、自包含、可直接运行的代码
# 必须保存预测结果到指定路径
```

## 验证结果（必须来自 Step 2 的实际运行输出，禁止估算）
- 指标名: {{ task_spec.metric_name if task_spec else "unknown" }}
- 指标值: （填写 Step 2 实际输出的数值）
- 提交路径: {{ workspace.working_dir if workspace else "." }}/submission.csv
