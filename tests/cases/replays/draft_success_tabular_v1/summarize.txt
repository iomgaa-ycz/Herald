## 高价值经验

1. **单模型优于简单集成**：LightGBM 单模型 AUC=0.9905 优于加权集成 0.9898，说明当模型间相关性高时，集成反而可能拉低强模型表现。**下轮应以最强单模型为基线，仅在模型差异度足够时才做集成。**

2. **XGBoost 拖后腿**：XGBoost AUC=0.9876 比 LightGBM 低约 0.003，加权集成未能有效利用互补性。**推断**：两模型学到的模式高度重叠，XGBoost 的错误并非 LightGBM 的正确区域的补充。

3. **制造控制数据的特征交互是关键**：任务目标明确提到"特征交互"，当前方案可能未充分挖掘高阶交互特征。

## 最终总结

| 维度 | 内容 |
|------|------|
| 方案 | LightGBM + XGBoost 加权集成，标准特征工程 |
| 最终提交指标 | 集成 AUC = 0.9898 |
| 最强单模型 | LightGBM AUC = **0.9905** |
| 执行状态 | 成功，exit_code=0，submission.csv 格式正确（100k 行） |
| 遗憾点 | 提交用了集成结果而非最强单模型，损失约 0.0007 |

**观察**：
- 三个模型/集成均超过 0.98，说明数据质量好、特征已有一定区分度
- 训练流程无报错，pipeline 稳定

**推断**：
- 0.99+ 的 AUC 说明大部分样本已被正确分类，剩余提升空间在困难样本的边界区域
- 特征工程（尤其是高阶交互）可能是突破 0.99 瓶颈的关键

## 下轮建议

### ✅ 保留
- LightGBM 作为主力模型，其超参配置作为基线
- 当前数据预处理与 CV 分割策略（结果稳定可复现）

### 🔧 修改
- **提交策略**：直接提交最强单模型预测，不做无差异度保障的集成
- **集成策略**：若要集成，加入差异性更大的模型（如 CatBoost、NN），并用 stacking 替代简单加权
- **特征工程**：
  - 增加成对特征交互（乘积、比值、差值）
  - 尝试 target encoding 对高基数特征
  - PCA/UMAP 降维后的成分作为辅助特征

### ➕ 补充
- 记录每个模型的 per-fold AUC 分布，观察方差以评估稳定性
- 输出 feature importance top-20，指导下轮特征工程方向
- 添加困难样本分析：预测概率在 0.4-0.6 区间的样本特征分布

## 迭代方向

1. **短期（下一轮）**：LightGBM 单模型 + 高阶特征交互 → 目标 AUC ≥ 0.992
2. **中期**：引入 CatBoost / TabNet 增加模型多样性 → stacking 集成
3. **长期**：伪标签 / 半监督 → 利用测试集分布信息进一步提升边界样本判别